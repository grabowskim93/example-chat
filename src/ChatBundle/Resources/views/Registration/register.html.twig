{% extends 'ChatBundle:Default:base.html.twig' %}

{% block body %}


    <div class="container">
        <div class="row">
            <div class="col-sm-8 col-md-4 col-md-offset-4">
                <h1 class="text-center login-title">Sign up</h1>
                    <img class="profile-img" src="{{ asset('bundles/images/chat.png') }}"alt="chat">

                   {#
                   {% form_theme form 'ChatBundle:Form:form_theme.html.twig' %}

                   {{ form_start(form) }}
                   {{ form_row(form.username) }}
                    {{ form_row(form.email) }}
                    {{ form_row(form.plainPassword.first) }}
                    {{ form_row(form.plainPassword.second) }}

                    <button type="submit">Create account!</button>
                    {{ form_end(form) }}#}
                    {% form_theme form 'ChatBundle:Form:form_theme.html.twig' %}

                    {{ form(form, {'attr':{'novalidate': 'true'}}) }}
                <a href="{{ path('login') }}">Back to login</a>
                <input type="hidden" name="_target_path" value="/homepage" />
            </div>
        </div>
    </div>

{% endblock %}